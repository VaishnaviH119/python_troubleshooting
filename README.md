# python_troubleshooting
A small, documented debugging exercise — diagnosing a runtime error from its traceback, identifying root cause, and fixing it using Python's logging module instead of a silent patch. Includes buggy and fixed versions plus a full write-up.
